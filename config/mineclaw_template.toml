[server]
host = "127.0.0.1"
port = 18789

[llm]
provider = "openai"
api_key = "${OPENAI_API_KEY}"
base_url = "https://api.openai.com/v1"
model = "gpt-4o"
max_tokens = 2048
temperature = 0.7

# 文件系统工具配置（本地工具始终启用）
[filesystem]
# 最大读取字节数（默认 16KB）
max_read_bytes = 16384
# 允许访问的目录列表（空表示允许所有）
allowed_directories = [
    # "/path/to/workspace",
]

# Checkpoint 配置
[checkpoint]
# 是否启用 checkpoint（默认 true）
enabled = true
# Checkpoint 存储目录（agentfs 路径，默认 .checkpoints）
checkpoint_directory = ".checkpoints"

# MCP (Model Context Protocol) 配置
# [mcp]
# enabled = true
#
# [[mcp.servers]]
# name = "filesystem"
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/workspace"]
# env = {}
#
# [[mcp.servers]]
# name = "github"
# command = "npx"
# args = ["-y", "@modelcontextprotocol/server-github"]
# env = { "GITHUB_PERSONAL_ACCESS_TOKEN" = "${GITHUB_TOKEN}" }
